import type { Metadata } from "next";
import { Outfit, Inter } from "next/font/google";
import "./globals.css";

const outfit = Outfit({
  variable: "--font-outfit",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800"],
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700"],
});

export const metadata: Metadata = {
  title: "Surya Industry | Premium Institutional Seating & Storage Solutions",
  description: "Providing high-quality, customized seating, desk systems, lockers, and storage cabinets for schools, corporate offices, healthcare facilities, banks, and government organizations across Karnataka.",
  keywords: ["institutional furniture", "office chairs", "school desks", "student desk and bench", "metal lockers", "storage cabinets", "Surya Industry", "Karnataka"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${outfit.variable} ${inter.variable} h-full antialiased scroll-smooth`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground font-sans">
        {children}
      </body>
    </html>
  );
}
